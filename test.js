
/**
 * @description The function does nothing. It logs the value 0 to the console but
 * never returns it. Since JavaScript functions always return the value of the last
 * expression evaluated within them (which is `console.log(0)`, which returns nothing),
 * the function returns `undefined`.
 */
const fn = () => { console.log(0); }

